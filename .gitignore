# Swift / SPM
.build/
.swiftpm/
*.xcodeproj
*.xcworkspace
DerivedData/

# macOS
.DS_Store

# Local agent / editor state
.claude/
.vscode/
.idea/

# Captured audio and exported transcripts must never be committed
*.wav
*.caf
*.m4a
listnr_*.md
Transcripts/

# Release artifacts (built by .github/workflows/release.yml)
dist/
manual/

# Code-signing material: private keys, certificates, the App Store Connect
# notarization key, and the .p12 password. Committing any of it means revoking
# the certificates and starting over. See Signing/setup-signing.sh.
Signing/private/

# asciinema captures are kept (they are text and re-renderable), but scratch
# frames pulled out with ffmpeg are not
demo/frames/
